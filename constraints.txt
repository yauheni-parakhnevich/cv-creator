# Pin agent-framework and its transitive dependencies that have only
# pre-release versions on PyPI.  Without these pins pip's pre-release
# resolver picks up incompatible combinations (e.g. agent-framework-core
# 1.0.0rc1 which removed ChatAgent and broke opentelemetry-semantic-conventions-ai
# compatibility).
agent-framework==1.0.0b251120
agent-framework-core==1.0.0b251001
opentelemetry-semantic-conventions-ai==0.4.13
